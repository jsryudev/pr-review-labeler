export enum Inputs {
  RepoToken = 'repo-token',
  TargetApprovedCount = 'target-approved-count',
  LabelToBeAdded = 'label-to-be-added',
  LabelToBeRemoved = 'label-to-be-removed'
}

export enum States {
  APPROVED = 'APPROVED',
  Open = 'open'
}
